logging:
  level:
    ROOT: INFO
    org.hibernate.SQL: INFO
    de.seith-codecake.airbnbcloneback: INFO

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/airbnbCloneDB
    username: user
    password: password
  jpa:
    properties:
      hibernate:
        default_schema: airbnb_clone
  liquibase:
    contexts: prod
    default-schema: airbnb_clone